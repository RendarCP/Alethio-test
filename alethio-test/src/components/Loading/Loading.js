import React from 'react'

function Loading() {
  return(
    <div 
      style={{ 
        display: 'flex', 
        justifyContent: 'center',
        fontSize: 30,
        fontWeight: 'bold'
        }}>
      로딩중...
    </div>
  )
}

export default Loading